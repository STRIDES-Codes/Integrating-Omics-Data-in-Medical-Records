import 'package:flutter/material.dart';

class AdminViwe extends StatelessWidget {
  const AdminViwe({ Key? key, required this.token, required this.full_name , required this.content}) : super(key: key);
  final String token;
  final String full_name;
  final content;

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}