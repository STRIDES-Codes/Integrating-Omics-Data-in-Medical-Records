import 'package:flutter/material.dart';

class DoctorViwe extends StatelessWidget {
  const DoctorViwe({ Key? key, required this.token, required this.full_name , required this.content, required this.id}) : super(key: key);
  final String token;
  final String full_name;
  final content;
  final id;

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}